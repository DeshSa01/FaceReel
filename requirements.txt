# Full pinned dependency closure, matching the versions the .venv runs, so the
# container behaves identically to a local run and rebuilds are deterministic.
# Verified to resolve as prebuilt wheels for cp314/manylinux x86_64 -- nothing
# in here compiles at build time.
#
# opencv-python-headless, not opencv-python: identical cv2 API (FaceDetectorYN /
# FaceRecognizerSF included) without the GUI stack, so the image needs no
# libGL/libglib. Nothing in this app calls cv2.imshow.
#
# yt-dlp is deliberately absent -- see requirements-ytdlp.txt.

# direct
fastapi==0.139.0
uvicorn==0.49.0
opencv-python-headless==5.0.0.93
numpy==2.5.0
python-multipart==0.0.32

# transitive, pinned to keep a rebuild from silently picking up a new starlette
# or pydantic under a fastapi version that was never tested against it
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
click==8.4.2
h11==0.16.0
idna==3.18
pydantic==2.13.4
pydantic-core==2.46.4
starlette==1.3.1
typing-extensions==4.16.0
typing-inspection==0.4.2
